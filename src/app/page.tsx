export default function Home() {
  return (
    <main className="mx-auto flex min-h-screen max-w-3xl flex-col justify-center gap-5 px-6 py-16">
      <p className="text-sm font-semibold uppercase tracking-[0.28em] text-cyan-300">ORION integration baseline</p>
      <h1 className="text-4xl font-semibold tracking-tight sm:text-6xl">Incident coordination with accountable human verification.</h1>
      <p className="max-w-2xl text-lg leading-8 text-slate-300">
        The shared contracts, tenant authorization, agent runtime and durable worker are ready for feature-team integration. Product UI remains intentionally minimal until the approved design reference is supplied.
      </p>
    </main>
  );
}
